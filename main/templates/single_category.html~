{% extends 'base.html' %}
{% load static %}

{% block content %}


    <!--SCROLL TO TOP-->
    <a href="#home" class="scrolltotop"><i class="fa fa-long-arrow-up"></i></a>

    <!--START TOP AREA-->
    <header class="top-area single-page" id="home">
    	{% if item %}
    		<div class="top-area-bg" style=" background-image: url({{ item.image.url }});"></div>
    	{% else %}
    		<div class="top-area-bg" style=" background-image: url({% static 'img/slider/slider15.jpg' %});"></div>
    	{% endif %}
        <div class="header-top-area">
            <!--MAINMENU AREA-->
            <div class="mainmenu-area" id="mainmenu-area">
                <div class="mainmenu-area-bg"></div>
                {% include 'navbar.html' %}
            </div>

        <div class="welcome-area">
            <div class="area-bg"></div>
            <div class="container">
                <div class="row flex-v-center">
                    <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12">
                        <div class="welcome-text text-center">
                            <h2>{{ category }} Category</h2>
                            <ul class="page-location">
                                <li><a href="{% url 'index' %}">Home</a></li>
                                <li>/</li>
                                <li><a href="{% url 'category' %}">Categories</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <!--END TOP AREA-->

    <style type="text/css">
        .blog-image{
            transition: 2s;
            box-shadow: 1px 9px 40px 12px rgba(255,110,110,0.9);
            transform: scale(0.98);
            background-color: purple;
        }
        .blog-image:hover{
            box-shadow: -1px 9px 40px -12px rgba(255,110,110,0.9);
            transform: scale(1.25);
            transition: 2s;
        }
    </style>

    <!--BLOG AREA-->
    <section class="blog-area blog-page gray-bg section-padding">
        <div class="container">
            <div class="row">

                {% if items %}
                    {% for item in items %}

                        <div class="col-md-4 col-lg-4 col-sm-6 col-xs-12">
                            <div class="single-blog wow fadeInUp" data-wow-delay="0.2s">
                                <div class="blog-image">
                                    <img src="{{ item.image.url }}" alt="">
                                </div>
                                
                            </div>
                        </div>

                    {% endfor %}

                {% else %}
                <div class="col-md-4 col-lg-4 col-sm-6 col-xs-12">
                    <p>Sorry, Nothing Yet</p>
                </div>
                {% endif %}
            </div>

        </div>
    </section>
    <!--BLOG AREA END-->
{% endblock %}
